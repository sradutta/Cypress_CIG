///<reference types = "cypress"/>

it.only('testing US Xpress', () => {
    //cy.visit('https://www.careersingear.com/us-xpress')
    
    //cy.get('.company-name').should('contain', 'US Xpress')
    // cy.get('.job-listings').its('length').should('be.gt', 0)
    // cy.get('.job-listings').then(($el) => {
    //     const itemCount = Cypress.$($el).length
    //     cy.log(itemCount)
    // })
})